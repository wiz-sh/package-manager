{
    "name": "@wiz-sh/package-manager-workspace",
    "private": true,
    "license": "MIT",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "bun run --cwd packages/pm build && bun run --cwd packages/runtime build",
        "lint": "biome lint .",
        "lint:fix": "biome lint --write .",
        "format": "biome format --write .",
        "format:check": "biome format .",
        "typecheck": "tsc --noEmit",
        "test": "bun test",
        "test:unit": "bun test packages",
        "test:integration": "bun test tests/integration",
        "test:coverage": "bun test --coverage",
        "cloc": "bun x --package cloc cloc packages/*/src --include-ext=ts,json",
        "check": "biome check . && bun run typecheck && bun test",
        "check:fix": "biome check --write ."
    },
    "devDependencies": {
        "@biomejs/biome": "^2.2.0",
        "@wiz-sh/registry-client": "file:../registry/packages/registry-client",
        "bun-types": "^1.2.19",
        "typescript": "^5.9.2"
    },
    "overrides": {
        "@wiz-sh/registry-client": "file:../registry/packages/registry-client"
    },
    "packageManager": "bun@1.3.14"
}
